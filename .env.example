# Optional: the app runs in demo mode without this.
VITE_SUPABASE_URL=

# Optional: the app runs in demo mode without this.
VITE_SUPABASE_ANON_KEY=

# Optional: the app runs in demo mode without this.
VITE_APP_URL=http://localhost:5173
